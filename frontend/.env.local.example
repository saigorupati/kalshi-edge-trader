# ── Kalshi Edge Trader — Frontend env vars ────────────────────────
#
# Copy this file to .env.local for local development:
#   cp .env.local.example .env.local
#
# For Railway deployment, set these in the Railway dashboard
# (Variables tab on the trader-dashboard service).

# Base URL of the FastAPI backend (no trailing slash)
# Local:      http://localhost:8000
# Production: https://your-bot-service.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL of the FastAPI backend (no trailing slash)
# Local:      ws://localhost:8000
# Production: wss://your-bot-service.railway.app
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Dashboard password — required, no default (dashboard is locked if unset)
# Set this in Railway under the trader-dashboard service → Variables
DASHBOARD_PASSWORD=change_me
